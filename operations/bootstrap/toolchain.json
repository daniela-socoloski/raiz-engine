{
  "$comment": "Manifesto canonico da cadeia de ferramentas do Raiz Engine. Fonte unica de versoes: o bootstrap e o doctor leem daqui, nunca de valores embutidos no codigo.",
  "manifestVersion": 1,
  "updated": "2026-08-20",

  "platforms": {
    "supported": ["win32-x64"],
    "proven": "win32-x64",
    "note": "Windows 11 x64 e a primeira plataforma comprovada. macOS e Linux permanecem DEFER."
  },

  "repository": {
    "slug": "daniela-socoloski/raiz-engine",
    "visibility": "private",
    "defaultBranch": "main",
    "note": "Repositorio privado. O clone exige autenticacao."
  },

  "profiles": {
    "developer": {
      "description": "Maquina que constroi e evolui o Raiz Engine com VS Code, Codex e Claude Code.",
      "requires": ["git", "git-lfs", "gh", "powershell", "node", "uv", "python", "ffmpeg"],
      "optional": [],
      "clonesCorpus": true,
      "note": "clonesCorpus=true baixa os objetos LFS, cerca de 398 MB de casos de marca."
    },
    "creator": {
      "description": "Maquina que apenas usa o Cena Raiz Desktop instalado. Nao exige ambiente de desenvolvimento.",
      "requires": [],
      "note": "Atendida pelo diretorio out/creator/win32-x64: CenaRaizSetup.exe, RELEASES e o pacote .nupkg devem permanecer juntos. Construido e verificado localmente; prova em Windows limpo ainda pendente."
    }
  },

  "tools": {
    "git": {
      "min": "2.40.0",
      "check": ["git", "--version"],
      "parse": "git version (\\d+\\.\\d+\\.\\d+)",
      "winget": "Git.Git",
      "why": "Fronteira de versionamento e clone do monorepositorio."
    },
    "git-lfs": {
      "min": "3.0.0",
      "check": ["git", "lfs", "version"],
      "parse": "git-lfs/(\\d+\\.\\d+\\.\\d+)",
      "winget": "GitHub.GitLFS",
      "why": "OBRIGATORIO. O corpus de marcas e versionado por LFS. Sem ele o clone traz ponteiros, nao os assets.",
      "critical": true
    },
    "gh": {
      "min": "2.40.0",
      "check": ["gh", "--version"],
      "parse": "gh version (\\d+\\.\\d+\\.\\d+)",
      "winget": "GitHub.cli",
      "why": "Autenticacao oficial e acesso ao repositorio privado."
    },
    "powershell": {
      "min": "7.4.0",
      "check": ["pwsh", "--version"],
      "parse": "PowerShell (\\d+\\.\\d+\\.\\d+)",
      "winget": "Microsoft.PowerShell",
      "why": "Runtime do bootstrap canonico depois que o launcher minimo parte do Windows PowerShell 5.1."
    },
    "node": {
      "min": "20.0.0",
      "check": ["node", "--version"],
      "parse": "v(\\d+\\.\\d+\\.\\d+)",
      "winget": "OpenJS.NodeJS.LTS",
      "why": "Aplicativo Electron, scripts de build e ferramentas do repositorio."
    },
    "uv": {
      "min": "0.4.0",
      "check": ["uv", "--version"],
      "parse": "uv (\\d+\\.\\d+\\.\\d+)",
      "winget": "astral-sh.uv",
      "why": "Ambiente Python da skill e do WhisperX, resolvido pelos locks."
    },
    "python": {
      "min": "3.10.0",
      "max": "3.13.99",
      "check": ["py", "-3.12", "--version"],
      "parse": "Python (\\d+\\.\\d+\\.\\d+)",
      "winget": "Python.Python.3.12",
      "why": "Helpers da skill. Verifica explicitamente o Python 3.12, mesmo quando outra versao e o padrao do sistema."
    },
    "ffmpeg": {
      "min": "6.0",
      "check": ["ffmpeg", "-version"],
      "parse": "ffmpeg version (\\d+\\.\\d+)",
      "winget": "Gyan.FFmpeg",
      "why": "Em desenvolvimento o app usa o FFmpeg do PATH. Empacotado, usa o runtime proprio."
    }
  },

  "components": {
    "desktop": {
      "path": "apps/cena-raiz-desktop",
      "install": ["npm", "ci"],
      "verify": ["npm", "run", "typecheck"],
      "tests": ["test:timeline", "test:media", "test:helpers", "test:jcut", "test:direction-plan", "test:motion-registry", "test:member-auth-storage", "test:signing-env", "test:gpg-host"],
      "note": "Aplicativo desktop canonico apos a consolidacao estrutural."
    },
    "skill": {
      "path": "skills/cena-raiz",
      "install": ["uv", "sync", "--frozen"],
      "verify": ["uv", "run", "python", "-m", "unittest", "discover", "-s", "tests"],
      "note": "Skill audiovisual canonica apos a consolidacao estrutural."
    }
  },

  "pendingBeforeDistribution": [
    "prova de instalacao, repeticao, reparo e falha parcial em Windows 11 x64 limpo",
    "launcher oficial verificavel para a entrada de maquina limpa",
    "publicacao propria de runtimes e feed de update; canais do aplicativo permanecem desativados ate existirem",
    "assinatura de codigo",
    "icone do app sem a cor do produto original"
  ]
}
